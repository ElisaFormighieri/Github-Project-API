import { FiArrowLeft } from 'react-icons/fi';
import { Back } from '../../components/BackArrow/styles';

export default function BackButton() {
  return (
    <Back>
      <FiArrowLeft color="#FFFFFF" size={32} />
    </Back>
  );
}
